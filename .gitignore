# Dependencies
node_modules/
.pnpm-store/

# Build output
dist/
dist-ssr/
*.tsbuildinfo

# Vite / dev caches
.vite/
*.timestamp-*.mjs

# Tests / coverage
coverage/
*.lcov

# Playwright E2E
/test-results/
/playwright-report/
/playwright/.cache/
/blob-report/

# Environment — commit only .env.example
.env
.env.*
!.env.example
*.local

# Logs
logs/
*.log
npm-debug.log*
yarn-debug.log*
pnpm-debug.log*

# Editor / IDE
.vscode/*
!.vscode/extensions.json
.idea/
*.swp
*~

# OS
.DS_Store
Thumbs.db

# Claude Code — developer-local AI-agent tooling (skills, settings, hooks); not shared via the repo
.claude/

# MCP tool artifacts
.playwright-mcp/

# impeccable — developer-local design tooling; not shared via the repo
.impeccable/
.impeccable-live.json
.impeccable-live/

# fe-rail spec scratch — per-feature working doc (/fe-spec regenerates it at the
# repo root each feature; transient handoff, not a durable deliverable)
/feature.md
