# CLAUDE.md

All project conventions live in `AGENTS.md` (single source of truth — do not duplicate them here).
Tool-enforceable rules (lint/format/types) are in `biome.json` and `tsconfig.json`; run `pnpm check` and `pnpm typecheck`.

@AGENTS.md
